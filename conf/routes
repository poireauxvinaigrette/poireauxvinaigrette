# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET   /carto                        controllers.Oumanger.map()
GET   /carto/:lat/:lon              controllers.Oumanger.centeredMap(lat:Double,lon:Double)
GET   /restos/:dist/:lat/:lon       controllers.Oumanger.list(dist:Integer,lat:Double,lon:Double, format:String ?= null)
GET   /restos				        controllers.Oumanger.index()

GET     /twilio                     controllers.Twilio.index()

GET     /nexmoI                     controllers.Nexmo.index()
GET     /nexmo                      controllers.Nexmo.logMenu(msisdn: String, to: String, messageId: String, text: String, type: String)
POST    /newMenu                    controllers.Cuisiner.cuisine()
#GET     /client                     controllers.Cuisiner.setMenu()

# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Oumanger.map()

# Home page
GET        /              @controllers.ss.SecuredAplli.index
GET        /userAware           @controllers.ss.SecuredAplli.userAware
GET        /only-twitter        @controllers.ss.SecuredAplli.onlyTwitter
GET        /link-result         @controllers.ss.SecuredAplli.linkResult
GET        /current-user        @controllers.ss.SecuredAplli.currentUser

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
->     /auth                securesocial.Routes
